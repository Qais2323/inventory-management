services:

  backend:
    build: ./backend

    ports:
      - "8000:8000"

    environment:
      DATABASE_URL: ${DATABASE_URL}

  frontend:
    build: ./frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend